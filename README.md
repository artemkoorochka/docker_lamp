# <center style="color:green">Docker LAMP</center>

- Use docker hab - https://hub.docker.com
- Create docker-compose.yml
- Create Dockerfile

```
docker-compose build
``` 
Run by docker API php, db and phpMyAdmin
```
docker-compose pull
```
To exit
```
docker-compose stop
```
```
docker-compose up -d
```
